{% extends 'admin/uts/layout.html.twig' %}

{% block arianne %}
  {{ parent() }}
{% endblock arianne %}

{% block content %}
  <form method="GET" class="form-search">
    <div class="field">
      <input type="text" name="search" class="input" placeholder="Rechercher" value="{{ app.request.query.get('search') }}">
      <button type="submit" class="button outlined">OK</button>
      <a class="button" href="{{ path('admin_uts_create') }}">Ajouter</a>
    </div>
  </form>
  <table class="table table-striped is-fullwidth is-narrow">
    <thead>
      <tr>
        <th>Identifiant</th>
        <th>Libellé</th>
        <th></th>
      </tr>
    </thead>
    {% for u in uts %}
    <tr>
      <td>{{ u.id }}</td>
      <td>{{ u.name }}</td>
      <td class="action">
        <a class="button outlined" href="{{ path('admin_uts_edit', {id: u.id}) }}"> Modifier</a>
      </td>
    </tr>
    {% endfor %}
  </table>
  {% include('ui/pagination/paginator.html.twig') with {'pagination': pagination} %}
{% endblock %}