{% extends 'admin/uts/layout.html.twig' %}

{% block arianne %}
  {{ parent() }}
{% endblock arianne %}

{% block content %}

  <div class="modal-box">
    <div class="title">Alerte</div>
    <div class="content center">
      <p>
      Ce type de sol est référencé dans les arbres des zones ou territoires suivants. Il ne peut être supprimé.
      </p>
      <p>
        {% for u in ucs %}
        <strong>⋅ {{ u.name }}</strong><br>
        {% endfor %}
      </p>
    </div>

    <div class="buttons">
      <a class="button outlined" href="{{ path('admin_uts') }}">Retour</a>
    </div>
  </div>

{% endblock %}
