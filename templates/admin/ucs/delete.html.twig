{% extends 'admin/ucs/layout.html.twig' %}

{% block arianne %}
  {{ parent() }}
  <li class="active"><a href="{{ path('admin_ucs_edit', {id: ucs.id}) }}">{{ ucs.name }}</a></li>
{% endblock arianne %}

{% block content %}

  <div class="modal-box">
    <div class="title">Alerte</div>
    <div class="content center">
      <p>
      Attention, la suppression de cette unité cartographique entraine la perte des clefs de détermination associées.
      </p>
      <p>
        <a class="button is-outlined is-danger " href="{{ path('admin_ucs_delete_confirmed', {id : ucs.id}) }}">Supprimer quand même</a>

      </p>
    </div>

    <div class="buttons">
      <a class="button outlined" href="{{ path('admin_ucs') }}">Retour</a>
    </div>
  </div>

{% endblock %}
